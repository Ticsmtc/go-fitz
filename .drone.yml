---
kind: pipeline
type: docker
name: default

steps:
  - name: backend
    image: golang:1.14-buster
    commands:
      - uname -a
      - pwd
      - go env
      - go env -w CC="gcc -no-pie"
      - go get -tags nopie -t -v ./...
      - go build -tags nopie
      - go test -tags nopie