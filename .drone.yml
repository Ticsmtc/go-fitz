---
kind: pipeline
type: docker
name: default

steps:
  - name: backend
    image: golang
    commands:
      - uname -a
      - pwd
      - go env
      - go get -tags nopie -t -v ./...
      - go build -tags nopie
      - go test -tags nopie